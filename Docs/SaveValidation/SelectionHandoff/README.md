# 悬浮边框在滑动到位后交接

目标按钮保留点击前的悬浮亮边，移动框的位置与尺寸动画全部完成时才隐藏原边框。悬浮底色不变，分组选项没有按下位移。

隔离 Unity 2022.3.34f1c1 工程 Play Mode 检查通过，共 82 项。覆盖设置分类、日志筛选、世界线列表的悬浮、按下、点击、移动中移出鼠标和到位交接，以及暂停、快速改选、重复绑定、关闭重开、世界线滚动和原生底栏返回。测试使用隔离存储，没有操作玩家存档。

`checks.txt` 为检查结果；`source-check.txt` 核对测试脚本与主工程一致。`*-moving.png` 和 `*-arrived.png` 为真实 Canvas 渲染截图，分别显示交接途中和到位状态。

截图读回会拉长单帧，第一次探测因捕获占用一帧而未采样到中间状态。验证器改为在截图读回后等待一帧再触发点击，随后检查通过；没有延长运行时的 0.2 秒动效。原始帧及完整引擎日志保留在临时隔离工程 `SelectionHandoffEvidence` 和 `selection-handoff-final.log` 中。
